import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    boxes: {
        display: 'flex',
        height: '100%',
        flexWrap: 'wrap',
        alignItems: 'start',
        alignContent: 'start',
    }
})